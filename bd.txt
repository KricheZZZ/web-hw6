CREATE TABLE IF NOT EXISTS admin (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    login VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL
);

--  администратор (пароль: admin123)
INSERT INTO admin (login, password_hash) 
VALUES ('admin', '$2y$10$itWJIT7KA2xXMJgBdrVOWuP15KhKl2IFxPpHHkAzTe67R1f4Nm1By');
-- Это хеш для пароля 'admin123' 

как узнали хеш:
временный файл hash.php:
PHP<?php
echo password_hash('admin123', PASSWORD_DEFAULT);
?>

После его загрузки на сервер  в браузере, видим строку типа:
$2y$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
Скопируем.

обновляем запись в таблице admin в sql на сервере:

UPDATE `admin` 
SET `password_hash` = '$2y$10$itWJIT7KA2xXMJgBdrVOWuP15KhKl2IFxPpHHkAzTe67R1f4Nm1By' 
WHERE `login` = 'admin';



